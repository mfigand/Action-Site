== README

This App it's an action site.

Allows you to :

* Create, Edit and Update Users, Products, Bids and Reviews.

* Use postgres as DB, so You may need to run the server before using whit this
pg_ctl -D /usr/local/var/postgres -l /usr/local/var/postgres/server.log start
