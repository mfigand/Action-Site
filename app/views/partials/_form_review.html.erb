<div class="input-field">

  <%= form_for [current_user, @product, @review] do |f| %>

    <%= f.label :description %>
    <%= f.text_field :description, class: "validate" %>
    <br>
    <%= f.submit 'Save' %>

  <% end %>
  
</div>
